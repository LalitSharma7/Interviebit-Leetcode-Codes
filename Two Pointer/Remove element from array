int Solution::removeElement(vector<int> &A, int B) {
    
    int i=0, j=0;
    for(;i<A.size() && j<A.size();i++)
    { 
      if(A[i] != B)
     {
      A[j] = A[i];
       j++;

     }
   }
  return j;
    
}
